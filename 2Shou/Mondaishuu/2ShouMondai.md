# 2章 問題

## 2.1.1 ディレクトリの内容を表示
1. ディレクトリの内容を表示するコマンド

2. サブディレクトリも展開して表示するオプション

3. iノード番号を表示するオプション

4. ディレクトリを示すのはどちらか
```shell
-rw-rw-r-- 1 honjo honjo   73  3月 24 21:19 ls.md
drwxrwxr-x 3 honjo honjo 4096  3月 24 21:20 sub1
```

## 2.1.2 タイムスタンプの更新と表示
1. ファイルの内容を更新せずにタイムスタンプのみ更新するコマンド

2. 1のコマンドで指定したファイルが存在しない場合はどうなるか

## 2.1.3 ワイルドカードによるファイル指定
1. 指定した位置に任意の文字（0文字以上）

2. 指定した位置に任意の文字（1文字）

3. 指定した位置に[]内に含まれる文字

4. 指定した位置に[]内に含まれない文字

5. ブレース展開を使ってtest1.txt 〜 test5.txtの5つのファイルを作成する

## 2.1.4 ファイルのコピー/移動/削除
### コピー
1. ファイルをコピーするコマンド

2. ディレクトリをコピーするオプション

3. 強制的にコピーする（上書き時に確認メッセージを表示しない）オプション

### 移動
1. ファイルを移動するコマンド

2. 強制的に移動する（上書き時に確認メッセージを表示しない）オプション

### 削除
1. ファイルを削除するコマンド

2. ディレクトリを削除するオプション

3. 強制的に削除する（メッセージを表示しない）オプション

## 2.1.5 ディレクトリの作成と削除
### 作成
1. ディレクトリを作成するコマンド

2. 複数階層のディレクトリをまとめて作成するオプション

### 削除
1. ディレクトリを削除するコマンド

2. 複数ディレクトリを削除するオプション

3. 1のコマンドとrm -rの違い

## 2.1.6 ファイルのアーカイブと圧縮
### アーカイブ
1. ファイルのアーカイブを行うコマンド

2. アーカイブファイルの作成オプション

3. アーカイブファイルの展開オプション

4. アーカイブファイルの内容を表示するオプション

5. 処理したファイルの一覧を表示するオプション

6. ファイルを指定するオプション

7. 以下のファイルをtest.tarというファイル名でアーカイブを行う場合のコマンド例
```shell
test1.txt
test2.txt
```

### 圧縮
1. gzip形式で圧縮するコマンド

2. アーカイブファイルをgzip形式で圧縮するtarコマンドのオプション

3. 以下のファイルをtest2.tarというファイル名でアーカイブ・圧縮をまとめて行う場合のコマンド例
```shell
test2.txt
test3.txt
test4.txt
```
4. test2.tar.gzという圧縮されたアーカイブファイルに対して解凍・展開をまとめて行う場合のコマンド例

### パーティション領域の圧縮
1. パーティション領域をまるごとアーカイブ操作するコマンド

2. /dev/sda1領域をdev.datというファイルにアーカイブするコマンド例

## 2.1.7 ファイルの種類の特定
1. ファイルの種類を特定するコマンド

## 2.2.1 所有者とパーミッション
1. mkdirでディレクトリを作成する際にパーミッションを指定するオプション

## 2.2.2 パーミッションの変更
1. パーミッションを変更するコマンド

2. 指定したディレクトリ以下のファイル・サブディレクトリもすべて変更するオプション

3. test.txtというファイルに以下の権限をシンボルモードで与えるコマンド例
```
3-1. 所有者へ書き込みと実行権限を追加
3-2. 所有グループへ実行権限を削除
3-3. その他ユーザへ読み取り権限を削除
3-4. すべてのユーザへ読み取り・書き込み・実行権限を追加
```

## 2.2.3 所有者/所有グループの変更
### 所有者
1. ファイル/ディレクトリの所有者を変更するコマンド

2. 指定したディレクトリ以下のファイル/サブディレクトリもすべて変更するオプション

3. test1.txtというファイルの所有者をrootユーザに変更するコマンド例

### 所有グループ
1. ファイル/ディレクトリの所有グループを変更するコマンド

2. 指定したディレクトリ以下のファイル/サブディレクトリもすべて変更するオプション

3. test1.txtというファイルの所有グループをusersというグループに変更するコマンド例

4. 3の操作をchownコマンドで行う場合のコマンド例（ファイルの所有者はtestuserとする）

## 2.2.4 マスク値の設定
1. マスク値を確認/設定するコマンド

2. ディレクトリの既定パーミッション

3. ファイルの既定パーミッション

4. マスク値を022に設定するコマンド例

## 2.2.5 特殊なパーミッション
### SUID
1. SUIDが設定されているファイル（プログラム）はどのような権限が与えられるか

2. SUIDが設定されているファイルの見分け方

3. ファイルにSUIDを設定するコマンド例

### SGID
1. SGIDが設定されているファイル（プログラム）はどのような権限が与えられるか

2. SGIDが設定されているファイルの見分け方

3. ファイルにSGIDを設定するコマンド例

### スティッキービット
1. スティッキービットを設定したファイルにはどのような効果があるか

2. スティッキービットを設定したファイルの見分け方

3. ファイルにスティッキービットを設定するコマンド例

## 2.3.2 リンクの仕組みと作成方法
1. リンクを作成するコマンド

2. シンボリックリンクを作成するオプション

3. リンク先が存在しても強制的に上書きして設定するオプション

4. moto.txtというファイルへのシンボリックリンクsaki.lnを作成するコマンド例

5. シンボリックリンクとハードリンクの見分けかた（3つ）

6. リンクとコピーの違い

7. リンク先ファイルを削除した際のハードリンクとシンボリックリンクの違い

8. シンボリックリンクにできて、ハードリンクにできないこと（2つ）

## 2.4.1 ファイル/ディレクトリの配置
### FHSで定義されるディレクトリ
1. ブート時に利用されるファイルを配置するディレクトリ

2. デバイスファイルを配置するディレクトリ

3. 設定ファイルを配置するディレクトリ

4. ライブラリファイルを配置するディレクトリ

5. 一般ユーザー権限で実行できる基本コマンドの実行ファイルを配置するディレクトリ

6. システム管理系のコマンドの実行ファイルを配置するディレクトリ

7. 一般ユーザー用のホームディレクトリ

8. ファイルシステムを一時的にマウントする領域

9. カーネルやプロセスの状態に関するファイルを配置するディレクトリ

10. ユーザーが共有するファイル（インストールされたアプリケーションなど）を格納するディレクトリ

11. 可変ファイル（ログ、メールなど）を格納するディレクトリ

12. リムーバブルメディア（DVDメディアなど）をマウントする領域

## 2.4.2 ファイルの検索
1. ファイルを検索するコマンド

2. 検索するサブディレクトリの階層を指定するオプション

3. 主な条件式
* 3-1. ファイルやディレクトリ名で検索
* 3-2. 更新日時で検索
* 3-3. 所有者名で検索
* 3-4. パーミッション値で検索
* 3-5. ファイルの種類で検索

5. 主なアクション
* 5-1. 検索結果の属性情報を表示するアクション
* 5-2. 検索結果に対して指定したコマンドを実行

6. 以下の検索コマンド例
* 6-1. カレントディレクトリ以下からtest2.txtという名前のファイルを検索
* 6-2. カレントディレクトリ以下からtest*.txtという名前のファイルを検索（ワイルドカードでファイル名を指定）
* 6-3. 更新日時が過去5日以内のファイルを検索
* 6-4. 更新日時が5日より以前のファイルを検索
* 6-5. test*.txtというファイルを検索して同時に削除する
